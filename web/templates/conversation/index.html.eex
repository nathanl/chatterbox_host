<h1>Ongoing Conversations</h1>

<ul>
  <%= Enum.map(@conversations, fn (conversation) -> %>
  <li><%= link("Conversation #{conversation.id}", to: "/conversations/#{conversation.id}") %></li>
  <% end) %>
</ul>
